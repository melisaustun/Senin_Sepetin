<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.irzstudio.SeninSepetin">

    <application
        android:name="com.irzstudio.SeninSepetin.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodApp"
        android:windowSoftInputMode="stateAlwaysHidden|adjustPan"
        tools:ignore="MissingClass">
        <activity
            android:name=".ui.activity.CheckoutActivity"
            android:exported="false" />
        <activity android:name=".ui.product.ProductActivity" />
        <activity android:name=".ui.activity.PasswordActivity" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan" />
        <activity android:name=".ui.activity.RegistrasiActivity" />
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.detailproduct.DetailProductActivity" />
        <activity
            android:name=".ui.activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>